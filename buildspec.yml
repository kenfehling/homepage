version: 0.1

phases:
  install:
    commands:
      - npm install
  pre_build:
    commands:
      - npm test
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - rm -rf node_modules
artifacts:
  files:
    - ./**/*
#  discard-paths: yes
  base-directory: server